# These packages exist in the base python:3.14-slim interpreter (Debian system Python,
# /usr/lib/python3/dist-packages/). They are NOT part of the application venv and are
# never loaded at runtime (app runs exclusively from /venv). The new versions are already
# installed in /usr/local/lib/python3.14/site-packages/ via the Dockerfile upgrade step.

# msgpack: Out-of-bounds read on Unpacker reuse (fixed in 1.2.1)
GHSA-6v7p-g79w-8964 pkg:pypi/msgpack@1.1.2

# setuptools: Path Traversal in PackageIndex (fixed in 78.1.1)
CVE-2025-47273 pkg:pypi/setuptools@70.3.0
